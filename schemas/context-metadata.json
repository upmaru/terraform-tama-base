{
  "title": "context-metadata",
  "description": "A context metadata is a metadata about a context.",
  "type": "object",
  "properties": {
    "origin_entity_identifier": {
      "type": "string",
      "description": "The identifier of the origin message"
    },
    "actor_id": {
      "type": "integer",
      "description": "The id of the actor"
    },
    "current_timestamp": {
      "type": "string",
      "description": "The current timestamp"
    }
  },
  "required": ["origin_entity_identifier", "actor_id", "current_timestamp"]
}
